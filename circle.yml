dependencies:
  pre:
    - sudo apt-get update; sudo apt-get build-essential libssl-dev curl
    - curl https://raw.githubusercontent.com/creationix/nvm/v0.33.1/install.sh | bash
    - source ~/.profile
    - nvm --version
  override:
    - ./build.sh
    
test:
  override:
    - echo "No tests yet do this makes circleci not failing"